# .understandignore — 排除分析的文件/目录模式
# 语法：与 .gitignore 相同（globs，# 注释，! 取反，末尾 / 表示目录）
# 取消注释某行以激活排除规则。
# 使用 ! 前缀可强制包含被默认规则排除的文件。
#
# 内置默认排除（始终生效，除非用 ! 取反）：
#   node_modules/, .git/, dist/, build/, obj/, *.lock, *.min.js, 等
#
# --- 测试文件模式（取消注释以排除）---

# *.test.*
# *.spec.*
# *.snap
